<header>
  <div class="title">
    <%= link_to project.title, chapter_project_path(project.chapter, project), :class => "title" %>
    <% if local_assigns[:moderation].present? && project.project_moderation %>
      <%= render "projects/moderation", project: project %>
    <% end %>
  </div>

  <section class="meta-data">
    <%= project.created_at %> (<%= project.chapter.display_name %>)

    <% if project.winner? %>
      · <%= link_to I18n.t(".view-public-page", scope: "projects.project"), funded_project_path(project), target: "blank" %>
    <% end %>
  </section>

  <section class="contact">
    <span>
      <%= project.name %>
    </span>

    <span>
      · <%= mail_to project.email %>
    </span>

    <% if project.phone.present? %>
      <span>
        · <%= project.phone %>
      </span>
    <% end %>

    <% if project.url.present? %>
      <span>
        · <%= link_to project.url, project.url %>
      </span>
    <% end %>
  </section>
</header>

