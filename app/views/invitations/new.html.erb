<%= simple_form_for [@chapter, @invitation] do |form| %>
  <%= form.input :chapter_id, :as => :select, :collection => Chapter.all, :group_method => :name %>
  <%= form.input :first_name %>
  <%= form.input :last_name %>
  <%= form.input :email %>
  <%= submit_tag "Invite" %>
<% end %>
